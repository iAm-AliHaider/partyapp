# ===========================================
# PartyApp - Vercel Environment Variables
# ===========================================
# INSTRUCTIONS:
# 1. Go to Vercel Dashboard → Your Project → Settings → Environment Variables
# 2. Add each of these variables
# 3. For DATABASE_URL: Go to Storage tab → Create Neon Postgres → it auto-fills
# ===========================================

# Database (Vercel Postgres / Neon)
# Option 1: Let Vercel auto-populate by adding Neon Postgres from Storage tab
# Option 2: Use your own Neon DB from https://neon.tech (free tier)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"

# NextAuth
NEXTAUTH_SECRET="replace-with-a-secure-random-string-min-32-chars"
NEXTAUTH_URL="https://partyapp.vercel.app"

# App URL (used for links, sharing, etc.)
NEXT_PUBLIC_APP_URL="https://partyapp.vercel.app"

# Webhook (for agent/automation integration)
WEBHOOK_API_KEY="siyasat-agent-key-2026"
